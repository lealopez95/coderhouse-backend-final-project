export class EntityProviderInterface { // @todo: transform this into an interface with typescript
    getAll = async () => { }

    save = async (item) => { }

    findById = async (id) => { }

    deleteById = async (id) => { }
}